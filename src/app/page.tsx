/**
 * Phase 0 placeholder. Replaced in Phase 2, when the content layer exists.
 * Deliberately carries no marketing copy — that lives in src/content/ (CLAUDE.md).
 */
export default function HomePage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-24">
      <h1 className="text-3xl font-semibold tracking-tight text-balance">
        Huronia Auto Glass
      </h1>
      <p className="text-muted mt-4">Phase 0 foundation. No content layer yet.</p>
    </main>
  )
}
